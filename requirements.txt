aiogram==3.7.0
aiohttp>=3.9.0,<4.0
# Должно совпадать с зависимостью aiogram-newsletter (apscheduler==3.10). Не ставить >=3.10.4 — ломает pip.
APScheduler==3.10.0
aiogram-newsletter==0.0.12
cachetools==5.3.2
environs==14.1.1
pydantic==2.5.3
redis==5.0.1
# APScheduler 3.10 тянет pkg_resources; в python:*-slim setuptools нет по умолчанию
setuptools>=69.0.0,<82.0.0
